"""Dependency-free management and World Model UI served by the Body host."""

BODY_GUI_HTML = r'''<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>Body Runtime · Embodied World Model</title>
<style>
:root{color-scheme:dark;font:14px Inter,ui-sans-serif,system-ui,sans-serif;background:#111613;color:#dce6df;--line:#303b33;--muted:#8e9e92;--green:#77d6a1;--cyan:#69cbd0;--panel:#171e19;--ink:#07120f}*{box-sizing:border-box}body{margin:0;min-width:320px;background:#111613}.shell{max-width:1540px;margin:0 auto;padding:28px 38px 48px}.eyebrow,.section-label{color:#76d9db;font:11px ui-monospace,monospace;letter-spacing:.08em}.topbar{display:flex;justify-content:space-between;align-items:flex-start;gap:24px;border-bottom:1px solid var(--line);padding-bottom:20px}.title h1{font-size:28px;font-weight:480;margin:8px 0 5px}.title p{color:var(--muted);font-size:12px;line-height:1.5;margin:0;max-width:680px}.top-actions{display:flex;align-items:center;justify-content:flex-end;gap:8px;flex-wrap:wrap}.status-line{color:#a9c0b2;font-size:11px;margin-right:4px}button,.nav-link{display:inline-flex;align-items:center;justify-content:center;gap:7px;border:1px solid #416b54;border-radius:4px;background:#14251b;color:#d9f5e5;padding:9px 12px;font:12px inherit;cursor:pointer;text-decoration:none}button:hover,.nav-link:hover{background:#1c3827;border-color:#70b58c}button:disabled{opacity:.45;cursor:wait}.nav{display:flex;gap:6px;margin:16px 0 22px;border-bottom:1px solid var(--line)}.nav button{background:transparent;border:0;border-bottom:2px solid transparent;color:#8fa297;padding:10px 13px;border-radius:0}.nav button.active{color:#b9f3cd;border-bottom-color:var(--green)}.view[hidden]{display:none}.stats{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:9px;margin:20px 0 14px}.stat{min-width:0;border:1px solid #304137;border-top:2px solid var(--cyan);background:#101b16;padding:12px 14px}.stat span{display:block;color:#8ca79a;font-size:10px;text-transform:uppercase}.stat b{display:block;margin-top:7px;color:#e1eee5;font:15px ui-monospace,monospace;overflow-wrap:anywhere}.goal-success{display:flex;align-items:center;gap:12px;margin:16px 0;padding:13px 16px;border:1px solid #5cb980;background:#153824;color:#d8ffe5}.goal-success[hidden]{display:none}.success-mark{display:grid;place-items:center;width:34px;height:34px;border-radius:50%;background:#69d393;color:#082015;font-size:20px;font-weight:700}.success-copy{display:grid;gap:3px}.success-copy strong{font-size:14px}.success-copy span,.success-meta{font-size:11px;color:#a8daba}.success-meta{margin-left:auto;white-space:nowrap}.panel{min-width:0;border:1px solid var(--line);background:var(--panel);padding:16px}.panel-head{display:flex;align-items:baseline;justify-content:space-between;gap:12px;margin:0 0 11px}.panel-head h2{margin:0;color:#9cd8b3;font:11px ui-monospace,monospace;letter-spacing:.06em;text-transform:uppercase}.panel-head small,.muted{color:var(--muted);font-size:11px}.map-panel{margin-bottom:14px}.map-intro{color:var(--muted);font-size:11px;line-height:1.5;margin:0 0 12px}.map-layout{display:grid;grid-template-columns:minmax(0,1fr) 220px;gap:16px;align-items:stretch}.map-wrap{position:relative;min-height:420px;padding:12px;border:1px solid #315044;background:#07120f radial-gradient(ellipse at 50% 45%,#183a2c55,transparent 65%)}svg.map{display:block;width:100%;height:100%;min-height:390px;max-height:650px}.room{fill:#0a1915;stroke:#68a888;stroke-width:.07}.gridline{stroke:#18372d;stroke-width:.025}.goal-ring{fill:none;stroke:#e5d17b;stroke-width:.09;stroke-dasharray:.18 .1}.reach{fill:#78d6ad12;stroke:#78d6ad63;stroke-width:.035;stroke-dasharray:.1 .08}.object{fill:#9bb8aa;stroke:#07120f;stroke-width:.07}.object.target{fill:#efb17d}.object.obstacle{fill:#a17c9f}.object.mobile{fill:#d588c2}.object.table{fill:#d6c477}.object.chair{fill:#8bb2d2}.body-dot{fill:#b6f1d3;stroke:#10291f;stroke-width:.08}.heading{fill:#edfff4}.map-label{fill:#d8eee1;font: .25px ui-monospace,monospace;paint-order:stroke;stroke:#07120f;stroke-width:.055}.scene-legend{display:flex;flex-direction:column;gap:0;max-height:440px;overflow:auto;padding-right:4px}.legend-row{display:grid;grid-template-columns:27px minmax(0,1fr);gap:8px;align-items:center;border-bottom:1px solid #29382f;padding:8px 0}.legend-key{display:grid;place-items:center;width:25px;height:25px;border-radius:50%;background:#829e8e;color:#08130e;font:11px ui-monospace,monospace;font-weight:700}.legend-key.body{background:#b6f1d3}.legend-key.target{background:#efb17d}.legend-key.obstacle{background:#a17c9f}.legend-key.mobile{background:#d588c2}.legend-key.table{background:#d6c477}.legend-key.chair{background:#8bb2d2}.legend-row b{display:block;color:#cce4d5;font-size:11px;font-weight:550}.legend-row small{display:block;color:#81978a;font-size:10px;margin-top:2px}.legend-foot{margin-top:auto;padding-top:10px;color:#779386;font-size:10px;line-height:1.5}.truth-note{margin-top:10px;border-left:2px solid #47775a;padding:5px 0 5px 10px;color:#8da99a;font-size:10px;line-height:1.45}.truth-note b{display:block;color:#b6d0c0;margin-bottom:3px}.detail-grid{display:grid;grid-template-columns:minmax(0,1fr) minmax(0,1fr);gap:14px}.detail-block+.detail-block{margin-top:20px}.meta{display:flex;flex-wrap:wrap;gap:7px;margin-bottom:10px}.meta span{padding:5px 7px;border:1px solid #30443b;color:#8ea89a;font-size:10px}.meta b{color:#cce4d5;font-weight:500}.perception-text{margin:0;color:#bfcec4;font-size:12px;line-height:1.6}.object-list,.event-list{max-height:260px;overflow:auto}.row{display:flex;justify-content:space-between;align-items:center;gap:14px;border-top:1px solid #29382f;padding:9px 1px;color:#c6d8cc;font-size:11px}.row small{display:block;color:#83988b;margin-top:3px}.row strong{font:11px ui-monospace,monospace;color:#b9d9c7;white-space:nowrap}.json{margin:0;max-height:260px;overflow:auto;white-space:pre-wrap;overflow-wrap:anywhere;background:#081019;color:#a8d9c2;padding:12px;border:1px solid #233a31;font:10px/1.5 ui-monospace,monospace}.world-layout{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px}.wide{grid-column:1/-1}.plugin-list{display:grid;gap:6px}.plugin{display:flex;align-items:center;justify-content:space-between;gap:12px;border-top:1px solid #29382f;padding:10px 2px}.plugin-info{display:grid;grid-template-columns:10px minmax(0,1fr);gap:8px;align-items:center}.dot{width:8px;height:8px;border-radius:50%;background:#68776d}.dot.on{background:#76e0a6;box-shadow:0 0 0 3px #76e0a622}.plugin-info b{font-size:12px;font-weight:550}.plugin-info small{grid-column:2;color:#82978b;font-size:10px}.plugin input{width:18px;height:18px;accent-color:#76d6a1}.plugin-settings{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px;margin-top:16px;padding-top:15px;border-top:1px solid var(--line)}.plugin-settings[hidden]{display:none}.field{display:grid;gap:6px;color:#91a99a;font-size:10px}.field input{min-width:0;width:100%;border:1px solid #3a4a3e;border-radius:4px;background:#101914;color:#dce8df;padding:9px;font:11px ui-monospace,monospace}.field.wide{grid-column:1/-1}.field.check{display:flex;align-items:center;gap:8px}.field.check input{width:16px;height:16px}.plugin-actions{display:flex;gap:8px;margin-top:12px}.map-modal{position:fixed;inset:0;z-index:10;display:none;place-items:center;background:#020806ee;padding:24px}.map-modal.open{display:grid}.modal-inner{width:min(96vw,1450px);height:min(94vh,1050px);display:flex;flex-direction:column;border:1px solid #4c896b;background:#0b1812;padding:16px}.modal-head{display:flex;justify-content:space-between;align-items:center;margin-bottom:10px}.modal-head h2{font:11px ui-monospace,monospace;color:#9cd8b3}.modal-layout{display:grid;grid-template-columns:minmax(0,1fr) 250px;gap:16px;flex:1;min-height:0}.modal-layout svg{min-height:0;max-height:none}.modal-close{font-size:19px;padding:5px 10px}.message{min-height:16px;color:#a7bcae;font-size:11px}.goal-achieved-page .goal-success{margin-top:18px}@media(max-width:900px){.shell{padding:22px 20px}.stats{grid-template-columns:repeat(3,minmax(0,1fr))}.map-layout{grid-template-columns:minmax(0,1fr) 190px}.map-wrap{min-height:340px}.detail-grid,.world-layout{grid-template-columns:1fr}}@media(max-width:620px){.shell{padding:18px 13px 32px}.topbar{display:block}.top-actions{justify-content:flex-start;margin-top:14px}.stats{grid-template-columns:repeat(2,minmax(0,1fr));gap:7px}.stat{padding:10px}.stat b{font-size:13px}.map-layout,.modal-layout{grid-template-columns:1fr}.map-wrap{min-height:290px;padding:7px}svg.map{min-height:270px}.scene-legend{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:0;max-height:230px}.legend-foot{grid-column:1/-1}.success-meta{display:none}.panel{padding:13px}.plugin-settings{grid-template-columns:1fr}.field.wide{grid-column:auto}.map-modal{padding:8px}.modal-inner{width:98vw;height:96vh;padding:10px}.modal-layout{overflow:auto}.modal-layout svg{min-height:50vh}}
.fnk-preview{border:1px solid #315443;background:linear-gradient(135deg,#0c1c15,#121b23);padding:15px;margin-bottom:14px}.fnk-preview-head{display:flex;justify-content:space-between;align-items:center;gap:12px}.fnk-preview-head>span:first-child{display:grid;gap:4px}.fnk-preview-head b{font-size:13px;color:#d5f1df}.fnk-preview-head small{font-size:10px;color:#8ea99a}.fnk-status{display:flex;align-items:center;gap:7px;color:#9cefc0;font:10px ui-monospace,monospace}.fnk-status i{width:7px;height:7px;border-radius:50%;background:#79e7aa;box-shadow:0 0 0 4px #79e7aa22;animation:fnk-pulse 1.2s infinite}.fnk-preview-grid{display:grid;grid-template-columns:minmax(250px,1fr) 220px;gap:16px;align-items:center;margin-top:10px}.fnk-preview-grid svg{display:block;width:100%;min-height:220px;background:radial-gradient(ellipse at center,#1b443222,transparent 68%)}.fnk-readouts{display:grid;grid-template-columns:1fr 1fr;gap:8px}.fnk-readouts div{border-left:2px solid #4c8f6d;padding:8px;background:#0b1712}.fnk-readouts small{display:block;color:#7f9b8b;font-size:10px}.fnk-readouts b{display:block;margin-top:5px;color:#c6f4d5;font:12px ui-monospace,monospace}.fnk-preview p{line-height:1.5;margin:4px 0 0}@keyframes fnk-pulse{50%{opacity:.35;transform:scale(.7)}}@media(max-width:650px){.fnk-preview-grid{grid-template-columns:1fr}.fnk-readouts{grid-template-columns:repeat(4,1fr)}}
</style>
</head>
<body>
<main class="shell">
<header class="topbar"><div class="title"><div class="eyebrow">COGNITIVE ORGANISM / BODY</div><h1 id="page-title">Independent Body Runtime</h1><p id="page-description">The Body owns perception, plugins, physical memory and action. The Brain consumes its observations through the connector.</p></div><div class="top-actions"><span id="message" class="status-line">Connecting to Body…</span><button id="run-toggle" onclick="toggleSimulation()">Start simulation</button><button onclick="step()">Step once</button><button onclick="resetModel()">Reset simulation</button><button onclick="refreshAll()" aria-label="Refresh">Refresh</button></div></header>
<nav class="nav"><button id="nav-body" onclick="showView('body')">Body &amp; plugins</button><button id="nav-world" onclick="showView('worldmodel')">Embodied World Model</button></nav>
<section id="view-body" class="view" hidden><div class="stats"><div class="stat"><span>Runtime</span><b id="runtime">—</b></div><div class="stat"><span>Plugins</span><b id="plugin-count">—</b></div><div class="stat"><span>Observations</span><b id="observation-count">—</b></div><div class="stat"><span>World model</span><b id="mode">—</b></div><div class="stat"><span>Source</span><b id="source">—</b></div><div class="stat"><span>Steps</span><b id="steps">—</b></div></div><section class="panel"><div class="panel-head"><h2>Plugins exposed to the Brain</h2><small>Configuration remains owned by the Body.</small></div><div id="plugins" class="plugin-list muted">Loading plugins…</div><div id="home-assistant-settings" class="plugin-settings" hidden><label class="field">Home Assistant URL<input id="ha-url" type="url" placeholder="http://homeassistant:8123"></label><label class="field">Long-lived access token<input id="ha-token" type="password" placeholder="Leave blank to keep current token"></label><label class="field">Polling interval (seconds)<input id="ha-poll" type="number" min="1" step="1"></label><label class="field">Allowed domains<input id="ha-domains" placeholder="Empty means all domains"></label><label class="field wide">Entities presented to the Brain<input id="ha-entities" placeholder="Comma-separated entity IDs"></label><label class="field check"><input id="ha-verify" type="checkbox"> Verify HTTPS certificate</label><div class="plugin-actions"><button onclick="saveHomeAssistantSettings()">Save settings</button><button onclick="discoverHomeAssistant()">Discover entities</button></div></div></section><section class="panel" style="margin-top:14px"><div class="panel-head"><h2>Latest Body observations</h2><small>Timestamped input stream</small></div><div id="observations" class="event-list muted">Waiting for Body observations.</div></section></section>
<section id="view-worldmodel" class="view" hidden><div id="goal-success" class="goal-success" role="status" hidden><div class="success-mark">✓</div><div class="success-copy"><strong>Goal achieved</strong><span id="success-description">Full task completed.</span></div><small id="success-meta" class="success-meta"></small></div><div class="stats"><div class="stat"><span>Mode</span><b id="wm-mode">—</b></div><div class="stat"><span>Source</span><b id="wm-source">—</b></div><div class="stat"><span>Stage</span><b id="wm-stage">—</b></div><div class="stat"><span>Steps</span><b id="wm-steps">—</b></div><div class="stat"><span>Runtime</span><b id="wm-running">—</b></div><div class="stat"><span>Prediction error</span><b id="wm-error">—</b></div></div><section class="panel map-panel"><div class="panel-head"><h2>Perception observatory</h2><button onclick="expandMap()">Expand map</button></div><p class="map-intro">Live Body position, orientation, objects, obstacles and current objective. The moving obstacle approaches the Body to exercise collision avoidance.</p><div class="map-layout"><div class="map-wrap"><svg id="map" class="map" viewBox="0 0 12 12" role="img" aria-label="Live Body perception map"></svg></div><div id="map-legend" class="scene-legend"><span class="muted">Waiting for scene.</span></div></div><div class="truth-note"><b>Perception boundary</b><span id="truth-note">Lumina receives the observation and affordances shown here.</span></div></section><div class="detail-grid"><section class="panel"><div class="panel-head"><h2>Body’s interpreted perception</h2><small id="perception-age">No observation</small></div><div class="meta"><span>Source <b id="perception-source">—</b></span><span>Objects <b id="perception-objects-count">—</b></span></div><p id="perception-text" class="perception-text">Waiting for the first Body observation.</p><div id="object-list" class="object-list" style="margin-top:12px"></div><div class="detail-block"><div class="panel-head"><h2>Affordances</h2></div><pre id="affordances" class="json">No affordances inferred yet.</pre></div></section><section class="panel"><div class="panel-head"><h2>Physical memory</h2><small id="anchor-count">—</small></div><pre id="memory" class="json">Loading physical memory…</pre><div class="detail-block"><div class="panel-head"><h2>Learned embodied concepts</h2></div><div id="concepts" class="event-list muted">No concepts consolidated yet.</div></div></section><section class="panel"><div class="panel-head"><h2>Last policy decision</h2></div><pre id="decision" class="json">No decision recorded.</pre></section><section class="panel"><div class="panel-head"><h2>Recent sensorimotor episodes</h2><small>Newest first</small></div><div id="episodes" class="event-list muted">No episodes recorded yet.</div></section></div></section>
<section id="fnk0031-panel" class="panel" style="margin-top:14px" hidden><div class="panel-head"><h2>FNK0031 Wi-Fi robot</h2><small>MuJoCo locomotion experiment · model geometry estimated pending kit calibration</small></div><div class="fnk-preview"><div class="fnk-preview-head"><span><b>Live controller telemetry</b><small id="fnk-controller-state">Waiting for World Model simulation</small></span><span class="fnk-status"><i></i>TRIPOD CPG · 18-NEURON SNN</span></div><div class="fnk-preview-grid"><svg id="fnk-preview" viewBox="0 0 360 230" role="img" aria-label="Hexapod gait and heading driven by controller outputs"></svg><div class="fnk-readouts"><div><small>CPG phase</small><b id="fnk-phase">—</b></div><div><small>Active spikes</small><b id="fnk-spikes">—</b></div><div><small>Verified walking</small><b id="fnk-competence">Not measured</b></div><div><small>Body heading</small><b id="fnk-heading">—</b></div><div><small>Controller steps</small><b id="fnk-steps">—</b></div></div></div><p id="fnk-controller-note" class="muted">The gait preview is telemetry. Run the separate physics-backed experiment to evaluate displacement, contacts, stability and falls.</p></div><section class="panel" style="margin:12px 0;padding:12px"><div class="panel-head"><h2>Physics-backed training and evaluation</h2><button id="fnk-experiment-run" onclick="runFnkExperiment()">Run bounded experiment</button></div><p class="muted">24 training episodes, then 8 paired randomized baseline/SNN tests. Runs asynchronously; no hardware commands are sent. A candidate result is not hardware validation. Model geometry remains estimated until calibrated on your kit.</p><pre id="fnk-experiment-status" style="max-height:260px;overflow:auto;white-space:pre-wrap;color:#b9d8c5;font:11px ui-monospace,monospace">No experiment run yet.</pre></section><div class="plugin-settings"><label class="field wide">FNK0031 / ESP32 bridge URL<input id="fnk0031-url" type="url" placeholder="http://fnk0031.local:9100"></label><label class="field">Access token<input id="fnk0031-token" type="password" placeholder="Leave blank to keep current token"></label><label class="field">Polling interval (seconds)<input id="fnk0031-poll" type="number" min="1" step="1"></label><label class="field">Request timeout (seconds)<input id="fnk0031-timeout" type="number" min="0.5" step="0.5"></label><label class="field check"><input id="fnk0031-snn" type="checkbox"> Enable SNN locomotion controller</label><label class="field check"><input id="fnk0031-actuation" type="checkbox"> Enable physical actuation</label><div class="plugin-actions"><button onclick="saveFnk0031Settings()">Save FNK0031 settings</button></div></div></section>
<div id="message-bottom" class="message" aria-live="polite"></div>
</main>
<div id="map-modal" class="map-modal" role="dialog" aria-modal="true" aria-label="Expanded Body world map"><div class="modal-inner"><div class="modal-head"><h2>Body spatial perception</h2><button class="modal-close" onclick="closeMap()" aria-label="Close map">×</button></div><div class="modal-layout"><svg id="map-large" class="map" viewBox="0 0 12 12" aria-label="Expanded perception map"></svg><div id="map-legend-large" class="scene-legend"></div></div></div></div>
<script>
const $=id=>document.getElementById(id);let currentView='body',lastStatus=null;
async function get(path){const r=await fetch(path,{cache:'no-store'});if(!r.ok)throw Error(path+' '+r.status);return r.json()}
async function post(path,payload={}){const r=await fetch(path,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});if(!r.ok)throw Error(path+' '+r.status);return r.json()}
const worldView=$('view-worldmodel'),mapPanel=worldView?.querySelector('.map-panel'),fnkPanel=$('fnk0031-panel');if(mapPanel&&fnkPanel)mapPanel.after(fnkPanel);
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}
function showView(view){currentView=view;$('view-body').hidden=view!=='body';$('view-worldmodel').hidden=view!=='worldmodel';$('nav-body').classList.toggle('active',view==='body');$('nav-world').classList.toggle('active',view==='worldmodel');$('page-title').textContent=view==='worldmodel'?'Embodied World Model':'Independent Body Runtime';$('page-description').textContent=view==='worldmodel'?'Live perception, physical memory, learned dynamics and action policy. The Body owns this model; the Brain reads it.':'The Body owns perception, plugins, physical memory and action. The Brain consumes its observations through the connector.';document.title=view==='worldmodel'?'Embodied World Model · Body':'Body Runtime';history.replaceState(null,'',view==='worldmodel'?'/worldmodel':'/');if(view==='worldmodel')void refreshWorld();else void refreshBody()}
function renderMap(w){const p=w?.perception||{},sim=w?.sim||{},simMode=w?.mode==='sim',simObjects=Object.entries(sim.objects||{}).map(([id,o])=>({id,label:id.replaceAll('_',' '),kind:o.kind,position:[o.x,o.y,0],size:id==='cup'?.4:.8})),objects=simMode?simObjects:(Array.isArray(p.objects)&&p.objects.length?p.objects:simObjects),width=Number(sim.width||12),height=Number(sim.height||12),body=simMode?(sim.body||[0,0,0]):(p.body?.position||sim.body||[0,0,0]),shelf=sim.shelf||[1,9],heading=simMode?Number(sim.heading_deg||0)*Math.PI/180:Number(p.body?.orientation??Number(sim.heading_deg||0)*Math.PI/180);const compassHeading=90-heading*180/Math.PI;const color=k=>k==='target'?'target':k==='obstacle'?'obstacle':k==='mobile_obstacle'?'mobile':k==='table'?'table':k==='chair'?'chair':'object';function draw(svg,legend){if(!svg)return;svg.setAttribute('viewBox',`0 0 ${width} ${height}`);const sy=y=>height-(Number(y)||0),sx=x=>Math.max(.25,Math.min(width-.25,Number(x)||0));let parts=[`<rect class="room" x="0" y="0" width="${width}" height="${height}"/>`];for(let i=0;i<=width;i++)parts.push(`<line class="gridline" x1="${i}" y1="0" x2="${i}" y2="${height}"/>`);for(let i=0;i<=height;i++)parts.push(`<line class="gridline" x1="0" y1="${i}" x2="${width}" y2="${i}"/>`);parts.push(`<circle class="goal-ring" cx="${sx(shelf[0])}" cy="${sy(shelf[1])}" r=".34"/><text class="map-label" x="${sx(shelf[0])+.42}" y="${sy(shelf[1])+.1}">goal</text>`);const items=[{code:'B',cls:'body',label:'Body',detail:`(${Number(body[0]).toFixed(1)}, ${Number(body[1]).toFixed(1)})`}];const reach=Number(p.body?.capabilities?.reach||1.8);parts.push(`<circle class="reach" cx="${sx(body[0])}" cy="${sy(body[1])}" r="${reach}"/>`);objects.forEach((o,i)=>{const q=o.position||[0,0],c=color(o.kind),r=Math.max(.22,Math.min(.56,Number(o.size||.4)/2)),label=esc(o.label||o.kind||`object ${i+1}`);parts.push(`<circle class="object ${c}" cx="${sx(q[0])}" cy="${sy(q[1])}" r="${r}"/><text class="map-label" text-anchor="middle" x="${sx(q[0])}" y="${sy(q[1])+.08}">${label}</text>`);items.push({code:String(i+1),cls:c,label:o.label||o.kind,detail:`${o.kind} · (${Number(q[0]).toFixed(1)}, ${Number(q[1]).toFixed(1)})`})});parts.push(`<g class="body-pose" transform="translate(${sx(body[0])} ${sy(body[1])}) rotate(${compassHeading})"><circle class="body-dot" r=".34"/><path class="heading" d="M0 0 L.72 -.19 L.72 .19 Z"/></g>`);svg.innerHTML=parts.join('');if(legend)legend.innerHTML=[...items,{code:'G',cls:'goal',label:'Goal',detail:`(${shelf[0]}, ${shelf[1]})`}].map(i=>`<div class="legend-row"><i class="legend-key ${i.cls}">${esc(i.code)}</i><span><b>${esc(i.label)}</b><small>${esc(i.detail)}</small></span></div>`).join('')+`<div class="legend-foot">${p.available?`Observation ${Number(p.age_seconds||0).toFixed(1)}s old · ${objects.length} objects`:`Simulator scene · ${objects.length} objects · near misses ${Number(sim.near_misses||0)}`}</div>`}draw($('map'),$('map-legend'));draw($('map-large'),$('map-legend-large'));$('truth-note').textContent=sim.objects?'Simulator scene: all objects are shown from ground truth; observations and affordances remain the policy inputs.':p.ground_truth_available?'Simulator source: the exact scene is shown for debugging; the policy receives the observation and affordances.':'No simulator ground truth is exposed for this source.'}
if(!document.getElementById('map-compass-style')){const style=document.createElement('style');style.id='map-compass-style';style.textContent='.compass-ring{fill:#0b1812;stroke:#89c9a3;stroke-width:.045}.compass-needle{fill:#aaf0c4;stroke:#07120f;stroke-width:.025}.compass-label{fill:#d8eee1;font: .28px ui-monospace,monospace;font-weight:bold;paint-order:stroke;stroke:#07120f;stroke-width:.05}';document.head.appendChild(style)}
const renderMapWithNorth=renderMap;renderMap=function(w){renderMapWithNorth(w);const NS='http://www.w3.org/2000/svg';for(const svg of [$('map'),$('map-large')]){if(!svg)continue;const [width,height]=svg.getAttribute('viewBox').split(/\s+/).slice(2).map(Number),x=width-.7,y=.85,g=document.createElementNS(NS,'g');g.setAttribute('class','map-compass');g.setAttribute('aria-label','North');const ring=document.createElementNS(NS,'circle');ring.setAttribute('cx',x);ring.setAttribute('cy',y+.18);ring.setAttribute('r','.38');ring.setAttribute('class','compass-ring');const needle=document.createElementNS(NS,'path');needle.setAttribute('d',`M ${x} ${y-.16} L ${x-.12} ${y+.13} L ${x} ${y+.07} L ${x+.12} ${y+.13} Z`);needle.setAttribute('class','compass-needle');const label=document.createElementNS(NS,'text');label.setAttribute('x',x);label.setAttribute('y',y-.25);label.setAttribute('text-anchor','middle');label.setAttribute('class','compass-label');label.textContent='N';g.append(ring,needle,label);svg.appendChild(g)}};
function renderWorld(w){lastStatus=w;const sim=w.sim||{},p=w.perception||{},goal=w.goal_status||{},plan=w.planning?.active_plan;$('wm-mode').textContent=w.mode||'—';$('wm-source').textContent=w.source?.source||'—';$('wm-stage').textContent=plan?`${plan.state} · ${plan.steps?.[plan.current_step_index]?.verb||'complete'}`:String(sim.task_stage||'—').replaceAll('_',' ');$('wm-steps').textContent=w.steps??0;$('wm-running').textContent=w.running?'Running':'Paused';$('wm-error').textContent=w.prediction_error_ema??'—';const achieved=plan?plan.state==='completed':Boolean(goal.achieved||sim.done);$('goal-success').hidden=!achieved;if(achieved){$('success-description').textContent=plan?`Plan completed: ${plan.objective}`:'Simulation objective achieved.';$('success-meta').textContent=plan?`${plan.steps?.length||0} verified steps`:`${goal.steps??sim.steps??0} steps · ${goal.successes??sim.successes??0} completed actions`}$('perception-age').textContent=p.available?`${Number(p.age_seconds||0).toFixed(1)}s old`:'No observation';$('perception-source').textContent=p.source||'—';$('perception-objects-count').textContent=p.objects?.length??0;$('perception-text').textContent=p.text||'Waiting for the first Body observation.';$('object-list').innerHTML=(p.objects||[]).map(o=>`<div class="row"><span><b>${esc(o.label)}</b><small>${esc(o.kind)} · ${(o.position||[]).slice(0,2).map(n=>Number(n).toFixed(1)).join(', ')}</small></span><strong>${Number(o.mass||0)} kg</strong></div>`).join('');$('affordances').textContent=p.affordances?JSON.stringify(p.affordances,null,2):'No affordances inferred yet.';const mem=w.memory||{};$('anchor-count').textContent=`${mem.lieux?.count||0} places · ${mem.objets?.count||0} objects`;$('concepts').innerHTML=(w.concepts?.top||[]).map(c=>`<div class="row"><span><b>${esc(c.label)}</b><small>${c.observations} observations · confidence ${c.confidence}</small></span><strong>${c.positive}/${c.negative}</strong></div>`).join('')||'<span class="muted">No concepts consolidated yet.</span>';$('decision').textContent=w.last_decision?JSON.stringify(w.last_decision,null,2):'No decision recorded.';const b=$('run-toggle');b.textContent=w.running?'Pause simulation':'Start simulation';b.dataset.running=String(Boolean(w.running));renderMap(w)}
async function refreshWorld(){try{const w=await get('/worldmodel/status');renderWorld(w);$('message').textContent=w.goal_status?.achieved?'Goal achieved':'Body connected'}catch(e){$('message').textContent='World model unavailable: '+e.message}}
async function refreshBody(){try{const h=await get('/health'),w=h.worldmodel||{};$('runtime').textContent=h.runtime||'—';$('plugin-count').textContent=`${(h.plugins||[]).filter(p=>p.enabled).length}/${(h.plugins||[]).length}`;$('observation-count').textContent=h.observations??0;$('mode').textContent=w.mode||'—';$('source').textContent=w.source?.source||'—';$('steps').textContent=w.steps??0;$('plugins').innerHTML=(h.plugins||[]).map(p=>`<label class="plugin"><span class="plugin-info"><i class="dot ${p.enabled?'on':''}"></i><b>${esc(p.label||p.id)}</b><small>${esc(p.role||'')}</small></span><input type="checkbox" ${p.enabled?'checked':''} onchange="togglePlugin('${esc(p.id)}',this.checked)"/></label>`).join('')||'<span class="muted">No plugins registered.</span>';const enabled=(h.plugins||[]).some(p=>p.id==='home_assistant'&&p.enabled);$('home-assistant-settings').hidden=!enabled;if(enabled&&!$('home-assistant-settings').dataset.loaded){await loadHomeAssistantSettings();$('home-assistant-settings').dataset.loaded='1'}const obs=await get('/observations');$('observations').innerHTML=(obs.observations||[]).slice(0,16).map(o=>`<div class="row"><span><b>${esc(o.subject)}</b><small>${esc(o.source)}${o.observed_at?' · '+new Date(o.observed_at*1000).toLocaleTimeString():''}</small></span><strong>${esc(o.value)}${o.unit?' '+esc(o.unit):''}</strong></div>`).join('')||'<span class="muted">No Body observations yet.</span>';$('message').textContent='Body connected'}catch(e){$('message').textContent='Body unavailable: '+e.message}}
async function refreshAll(){await(currentView==='worldmodel'?refreshWorld():refreshBody())}
async function toggleSimulation(){const running=$('run-toggle').dataset.running!=='true';try{await post('/worldmodel/run',{running,shuffle:running});await refreshWorld()}catch(e){$('message').textContent=e.message}}
async function step(){try{await post('/worldmodel/step');await refreshWorld()}catch(e){$('message').textContent=e.message}}
async function resetModel(){if(!confirm('Reset the simulation state?'))return;try{await post('/worldmodel/reset',{clear_memory:false});await refreshWorld()}catch(e){$('message').textContent=e.message}}
function expandMap(){$('map-modal').classList.add('open');document.body.style.overflow='hidden'}function closeMap(){$('map-modal').classList.remove('open');document.body.style.overflow=''}document.addEventListener('keydown',e=>{if(e.key==='Escape')closeMap()});
async function togglePlugin(id,enabled){try{await post('/plugins/'+id,{enabled});await refreshBody()}catch(e){$('message').textContent='Plugin update failed: '+e.message}}
async function loadHomeAssistantSettings(){try{const s=await get('/plugins/home_assistant/settings');$('ha-url').value=s.url||'';$('ha-poll').value=s.poll_interval||5;$('ha-domains').value=s.allowed_domains||'';$('ha-entities').value=s.entities||'';$('ha-verify').checked=!!s.verify_ssl;$('ha-token').placeholder=s.token_configured?'Token configured; leave blank to keep it':'Enter a Home Assistant token'}catch(e){$('message').textContent='Home Assistant settings unavailable: '+e.message}}
async function saveHomeAssistantSettings(){const payload={url:$('ha-url').value,token:$('ha-token').value,poll_interval:Number($('ha-poll').value||5),allowed_domains:$('ha-domains').value,entities:$('ha-entities').value,verify_ssl:$('ha-verify').checked};try{await post('/plugins/home_assistant/settings',payload);$('ha-token').value='';$('message').textContent='Home Assistant settings saved';await refreshBody()}catch(e){$('message').textContent='Home Assistant settings failed: '+e.message}}
async function discoverHomeAssistant(){try{const r=await post('/plugins/home_assistant/discover');$('message').textContent=r.ok?'Home Assistant discovery complete':(r.message||'Discovery failed');await refreshBody()}catch(e){$('message').textContent='Discovery failed: '+e.message}}
let fnkSettingsLoaded=false,fnkTickBusy=false,fnkVisualHeading=90;function drawFnkController(s){const svg=$('fnk-preview');if(!svg)return;const joints=s.joint_targets||[],cpg=s.cpg||[],spikes=s.spikes||Array(18).fill(0),active=Boolean(s.active);$('fnk-controller-state').textContent=active?`Controller running · ${s.mode}`:(s.reason||'Waiting for world model simulation');$('fnk-controller-note').textContent=active?`Gait sandbox · reward source: ${s.reward_source||'unverified'} · walking not validated${s.state_loaded?' · experimental weights restored':''}`:'Start World Model simulation and enable SNN locomotion to advance the controller.';if(!active){svg.innerHTML='';$('fnk-phase').textContent='—';$('fnk-spikes').textContent='—';$('fnk-competence').textContent='Not measured';$('fnk-heading').textContent='—';$('fnk-steps').textContent='—';return}const heading=Number(s.body_heading_deg||0),targetHeading=(90-heading+360)%360,delta=((targetHeading-fnkVisualHeading+540)%360)-180;fnkVisualHeading=(fnkVisualHeading+delta*.22+360)%360;const legs=Array.from({length:6},(_,i)=>{const side=i%2===0?-1:1,row=Math.floor(i/2),hipX=180+side*31,hipY=63+row*49,value=Number(joints[i]?.[0]||0),lift=Number(cpg[i]||0),kneeX=hipX+side*(26+value*6),kneeY=hipY+value*10,footX=kneeX+side*(25+Math.max(0,lift)*6),footY=kneeY+value*14,color=lift>=0?'#79e1aa':'#c8a6ec';return `<g opacity="${.78+Math.abs(lift)*.22}"><line x1="${hipX}" y1="${hipY}" x2="${kneeX}" y2="${kneeY}" stroke="${color}" stroke-width="8" stroke-linecap="round"/><line x1="${kneeX}" y1="${kneeY}" x2="${footX}" y2="${footY}" stroke="${color}" stroke-width="6" stroke-linecap="round"/><circle cx="${kneeX}" cy="${kneeY}" r="4" fill="#d6eee0"/><circle cx="${footX}" cy="${footY}" r="4.5" fill="${color}"/></g>`}).join('');const neuronDots=Array.from({length:18},(_,i)=>{const on=Boolean(spikes[i]),col=i<6?'#80dfaa':i<12?'#86cbd2':'#c6a2e8';return `<circle cx="${54+(i%9)*31.5}" cy="205" r="${on?4.2:2.4}" fill="${on?'#f0ca76':col}" opacity="${on?1:.55}"/>`}).join('');const tripodA=[0,3,4];const groupMarks=Array.from({length:6},(_,i)=>{const side=i%2===0?-1:1,row=Math.floor(i/2),x=180+side*31,y=63+row*49,group=tripodA.includes(i)?'A':'B';return `<text x="${x+side*12}" y="${y+4}" fill="${group==='A'?'#83e5ad':'#d1aff0'}" font-size="8" font-family="monospace">${group}</text>`}).join('');svg.innerHTML=`<ellipse cx="180" cy="112" rx="104" ry="95" fill="#48b77b" opacity=".035"/><g transform="rotate(${fnkVisualHeading} 180 112)">${legs}<path d="M154 48 Q180 34 206 48 L206 176 Q180 190 154 176 Z" fill="#152b22" stroke="#7ed9ab" stroke-width="2"/><text x="180" y="106" text-anchor="middle" fill="#8cae99" font-size="8" font-family="monospace">FRONT ↑</text><text x="180" y="121" text-anchor="middle" fill="#c8f5d9" font-size="10" font-family="monospace">FNK0031</text>${groupMarks}</g><text x="180" y="197" text-anchor="middle" fill="#829a8b" font-size="7" font-family="monospace">18 IZH NEURONS · LIVE SPIKES</text>${neuronDots}`;$('fnk-phase').textContent=`${Math.round(Number(s.cpg_phase||0)*180/Math.PI)}°`;$('fnk-spikes').textContent=`${spikes.filter(Boolean).length} / 18`;$('fnk-competence').textContent='Not measured';$('fnk-heading').textContent=`${Math.round(heading)}°`;$('fnk-steps').textContent=String(s.step||0)}
function syncMapPose(pose){if(!pose?.body)return;const x=Number(pose.body[0]),y=Number(pose.body[1]),angle=90-Number(pose.heading_deg||0);for(const svg of [$('map'),$('map-large')]){if(!svg)continue;const vb=svg.getAttribute('viewBox').split(/\s+/).map(Number),width=vb[2],height=vb[3],px=Math.max(.25,Math.min(width-.25,x)),py=height-Math.max(.25,Math.min(height-.25,y)),body=svg.querySelector('.body-pose'),reach=svg.querySelector('.reach');if(body)body.setAttribute('transform',`translate(${px} ${py}) rotate(${angle})`);if(reach){reach.setAttribute('cx',px);reach.setAttribute('cy',py)}}}
let fnkSpikeHoldUntil=Array(18).fill(0);const drawFnkControllerWithCompassHeading=drawFnkController;drawFnkController=function(s){if(s.active){fnkVisualHeading=(90-Number(s.body_heading_deg||0)+360)%360}drawFnkControllerWithCompassHeading(s);if(!s.active){fnkSpikeHoldUntil.fill(0);return}syncMapPose(s.worldmodel_pose);const heading=Number(s.body_heading_deg||0),now=performance.now(),spikes=s.spikes||[];$('fnk-heading').textContent=`${Math.round((90-heading+360)%360)}°`;$('fnk-heading').previousElementSibling.textContent='Compass heading · ° from N';$('fnk-controller-state').textContent=s.simulation_running===false?`World Model paused · last gait ${s.gait||'idle'} · map step ${s.worldmodel_pose?.step??'—'}`:s.gait==='idle'?`Controller holding posture · ${s.mode}`:`Controller gait ${s.gait} · ${s.mode}`;const nodes=Array.from($('fnk-preview').querySelectorAll('circle')).slice(-18);for(let i=0;i<18;i++){if(spikes[i])fnkSpikeHoldUntil[i]=now+320;const firing=fnkSpikeHoldUntil[i]>now;if(nodes[i]){nodes[i].setAttribute('fill',firing?'#f0ca76':i<6?'#80dfaa':i<12?'#86cbd2':'#c6a2e8');nodes[i].setAttribute('opacity',firing?'1':'.55');nodes[i].setAttribute('r',firing?'4.2':'2.4')}}const recent=fnkSpikeHoldUntil.filter(until=>until>now).length;$('fnk-spikes').textContent=`${recent} / 18`;$('fnk-spikes').previousElementSibling.textContent='Recent spikes'};
let fnkPausedStatusAt=0;async function tickFnkController(){if(fnkTickBusy||$('fnk0031-panel').hidden)return;if(!$('fnk0031-snn').checked){drawFnkController({active:false,reason:'Enable SNN locomotion in plugin settings'});return}fnkTickBusy=true;try{if(performance.now()-fnkPausedStatusAt>70){fnkPausedStatusAt=performance.now();drawFnkController(await get('/plugins/fnk0031_wifi/controller'))}}catch{}finally{fnkTickBusy=false}}
async function refreshFnkPanel(){try{const data=await get('/plugins'),plugin=(data.plugins||[]).find(p=>p.id==='fnk0031_wifi');$('fnk0031-panel').hidden=!plugin?.enabled;if(plugin?.enabled&&!fnkSettingsLoaded){const s=await get('/plugins/fnk0031_wifi/settings');$('fnk0031-url').value=s.url||'';$('fnk0031-poll').value=s.poll_interval||5;$('fnk0031-timeout').value=s.timeout||5;$('fnk0031-snn').checked=!!s.snn_enabled;$('fnk0031-actuation').checked=!!s.actuation_enabled;$('fnk0031-token').placeholder=s.token_configured?'Token configured; leave blank to keep it':'Optional access token';fnkSettingsLoaded=true}if(plugin?.enabled&&$('fnk0031-snn').checked)lastStatus=await get('/worldmodel/status')}catch{}}
async function saveFnk0031Settings(){const payload={url:$('fnk0031-url').value,token:$('fnk0031-token').value,poll_interval:Number($('fnk0031-poll').value||5),timeout:Number($('fnk0031-timeout').value||5),snn_enabled:$('fnk0031-snn').checked,actuation_enabled:$('fnk0031-actuation').checked};try{await post('/plugins/fnk0031_wifi/settings',payload);$('fnk0031-token').value='';fnkSettingsLoaded=false;$('message').textContent='FNK0031 settings saved';await refreshFnkPanel()}catch(e){$('message').textContent='FNK0031 settings failed: '+e.message}}
async function refreshFnkExperiment(){try{const s=await get('/plugins/fnk0031_wifi/experiment');const button=$('fnk-experiment-run');button.disabled=['starting','baseline','training','evaluation'].includes(s.state);button.textContent=button.disabled?`Experiment ${s.completed||0}/${s.total||18}`:'Run bounded experiment';$('fnk-experiment-status').textContent=JSON.stringify(s,null,2)}catch(e){$('fnk-experiment-status').textContent='MuJoCo experiment status unavailable: '+e.message}}
async function runFnkExperiment(){try{await post('/plugins/fnk0031_wifi/experiment/start',{});await refreshFnkExperiment()}catch(e){$('fnk-experiment-status').textContent='Could not start experiment: '+e.message}}
drawFnkController({active:false,reason:'Enable the plugin, enable SNN locomotion, then start World Model simulation'});setInterval(tickFnkController,50);setInterval(refreshFnkPanel,2500);setInterval(refreshFnkExperiment,2000);refreshFnkExperiment();
let pollTimer;async function poll(){await refreshAll();pollTimer=setTimeout(poll,currentView==='worldmodel'?900:3000)}
showView(location.pathname==='/worldmodel'?'worldmodel':'body');void poll();setInterval(()=>{if(currentView==='worldmodel')void refreshDetails()},4000);
async function refreshDetails(){try{const [a,e]=await Promise.all([get('/worldmodel/anchors'),get('/worldmodel/episodes?limit=12')]);$('memory').textContent=JSON.stringify(a,null,2);const steps=Array.isArray(e)?e:[];$('episodes').innerHTML=steps.slice().reverse().map(x=>`<div class="row"><span><b>${esc(x.action)}</b><small>step ${x.step} · reward ${x.reward} · error ${x.pred_error}</small></span><strong>${esc(x.outcome)}</strong></div>`).join('')||'<span class="muted">No episodes recorded yet.</span>'}catch{}}
</script></body></html>'''

BODY_GUI_HTML = BODY_GUI_HTML.replace(
    "</script></body></html>",
    """</script><script>
const baseFnkTelemetryDraw=drawFnkController;
drawFnkController=function(s){
  baseFnkTelemetryDraw(s);
  const note=document.getElementById('fnk-controller-note');
  if(!note)return;
  if(!s.active){note.textContent=s.reason||'Start the simulated World Model to run the local hexapod plant.';const oldMap=document.getElementById('fnk-servo-map');if(oldMap)oldMap.innerHTML='';return}
  const imu=s.imu||{},error=s.forward_prediction_error||[];
  note.textContent=`HEURISTIC PLANT (not physics) · pitch ${Number(imu.pitch||0).toFixed(3)} rad · roll ${Number(imu.roll||0).toFixed(3)} rad · synthetic reward ${Number(s.reward||0).toFixed(3)} · CPG ${(Number(s.cpg_weight||1)*100).toFixed(0)}% / SNN ${(Number(s.snn_weight||0)*100).toFixed(0)}% · prediction error ${Number(error[0]||0).toFixed(3)}, ${Number(error[1]||0).toFixed(3)} · walking not validated`;
  const svg=document.getElementById('fnk-preview');
  svg.setAttribute('viewBox','0 0 360 280');
  const legs=[
    {n:1,x:158,y:88,side:-1,group:'A'}, {n:2,x:153,y:115,side:-1,group:'B'},
    {n:3,x:158,y:142,side:-1,group:'A'}, {n:4,x:202,y:88,side:1,group:'B'},
    {n:5,x:207,y:115,side:1,group:'A'}, {n:6,x:202,y:142,side:1,group:'B'}
  ];
  const cpg=s.cpg||[],contact=s.contact||Array(6).fill(0),lift=s.foot_lift||Array(6).fill(0),footMotion=s.foot_motion||[];
  const legsSvg=legs.map((leg,i)=>{
    const sourceIndex=[0,2,4,1,3,5][i];
    const stride=Number(cpg[sourceIndex]||0),air=Number(lift[sourceIndex]||0),ground=Number(contact[sourceIndex]||0)===1;
    const motion=footMotion[sourceIndex]||[0,0,0],turning=String(s.gait||'').startsWith('turn_');
    const [coxa,femur,tibia]=(s.joint_targets||[])[sourceIndex]||[0,0,0];
    const coxaX=leg.x+leg.side*(11+(turning?0:Number(coxa||0)*3)),coxaY=leg.y+(turning?0:Number(coxa||0)*2);
    const femurX=coxaX+leg.side*(15+(turning?0:Number(femur||0)*3)),femurY=coxaY-(turning?0:stride*4)+Number(turning?0:femur||0)*3;
    const footX=femurX+leg.side*(15+air*5)+Number(turning?motion[0]||0:0)*22;
    const footY=femurY-(turning?0:stride*8)+Number(turning?0:tibia||0)*4+Number(turning?motion[1]||0:0)*22-air*(turning?12:0);
    const color=ground?'#75dda4':'#f0bd70',dash=ground?'':'stroke-dasharray="4 3"';
    return `<g><line x1="${leg.x}" y1="${leg.y}" x2="${coxaX}" y2="${coxaY}" stroke="#83cbd1" stroke-width="6" stroke-linecap="round"/><line x1="${coxaX}" y1="${coxaY}" x2="${femurX}" y2="${femurY}" stroke="#9ce0b4" stroke-width="6" stroke-linecap="round"/><line x1="${femurX}" y1="${femurY}" x2="${footX}" y2="${footY}" stroke="${color}" stroke-width="5" stroke-linecap="round" ${dash}/><circle cx="${coxaX}" cy="${coxaY}" r="3.2" fill="#d5f2f4"/><circle cx="${femurX}" cy="${femurY}" r="3.2" fill="#d9f4df"/><circle cx="${footX}" cy="${footY}" r="${4+air*2}" fill="${color}"/><text x="${footX+leg.side*8}" y="${footY-5}" text-anchor="${leg.side<0?'end':'start'}" fill="#dcebe2" font-size="9" font-family="monospace">L${leg.n}</text><text x="${leg.x+leg.side*11}" y="${leg.y+3}" fill="#8cae9a" font-size="7" font-family="monospace">${leg.group}</text></g>`;
  }).join('');
  const neurons=Array.from({length:18},(_,i)=>{const on=Boolean((s.spikes||[])[i]),leg=Math.floor(i/3),visualLeg=[0,3,1,4,2,5][leg],col=on?'#ffd27a':(contact[leg]?'#75dda4':'#f0bd70');return `<circle cx="${45+visualLeg*54+(i%3)*7}" cy="250" r="${on?4:2.5}" fill="${col}" opacity="${on?1:.65}"/>`}).join('');
  const contactByLeg=[contact[0],contact[2],contact[4],contact[1],contact[3],contact[5]];
  svg.innerHTML=`<text x="180" y="16" text-anchor="middle" fill="#dcebe2" font-size="9" font-family="monospace">N ↑</text><path d="M180 21l-5 9h10z" fill="#dcebe2"/><g transform="rotate(${fnkVisualHeading} 180 118)">${legsSvg}<rect x="155" y="70" width="50" height="96" rx="10" fill="#152b22" stroke="#7ed9ab" stroke-width="2"/><text x="180" y="106" text-anchor="middle" fill="#8cae99" font-size="8" font-family="monospace">FRONT</text><text x="180" y="121" text-anchor="middle" fill="#c8f5d9" font-size="9" font-family="monospace">FNK0031</text></g><text x="180" y="194" text-anchor="middle" fill="#a7beaf" font-size="8" font-family="monospace">${s.gait||'idle'} · stance ${contactByLeg.map((v,i)=>v?`L${i+1}`:'').filter(Boolean).join(' ')||'none'}</text><text x="180" y="210" text-anchor="middle" fill="#81998b" font-size="7" font-family="monospace">TRIPOD A: L1/L3/L5 · TRIPOD B: L2/L4/L6</text><text x="180" y="235" text-anchor="middle" fill="#81998b" font-size="7" font-family="monospace">18 Izhikevich neurons · recent firing</text>${neurons}`;
  let servoMap=document.getElementById('fnk-servo-map');
  if(!servoMap){servoMap=document.createElement('div');servoMap.id='fnk-servo-map';servoMap.style.cssText='display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:8px;margin:8px 0 12px;padding-top:10px;border-top:1px solid #304137';document.querySelector('#fnk0031-panel .fnk-preview').insertBefore(servoMap,note)}
  const servos=s.servo_targets||[];
  servoMap.innerHTML=`<div style="grid-column:1/-1;color:#8ea99a;font:10px ui-monospace,monospace">${s.servo_count||servos.length} LOGICAL SERVO TARGETS · 3 per leg · normalized command, not calibrated degrees or board channel wiring</div>`+legs.map(leg=>{const rows=servos.filter(x=>x.leg===`L${leg.n}`).map(x=>`<small style="display:block;color:#9eb6a7;font:9px ui-monospace,monospace">S${String(x.index).padStart(2,'0')} ${x.joint.slice(0,1).toUpperCase()} ${Number(x.target).toFixed(2)}</small>`).join('');return `<div style="padding:5px 2px;border-top:2px solid #47775a"><b style="display:block;color:#d3e8da;font:10px ui-monospace,monospace">L${leg.n}</b>${rows||'<small>Awaiting servo state</small>'}</div>`}).join('');
  if(!document.getElementById('fnk-servo-responsive')){const style=document.createElement('style');style.id='fnk-servo-responsive';style.textContent='@media(max-width:650px){#fnk-servo-map{grid-template-columns:repeat(3,minmax(0,1fr))!important}}';document.head.appendChild(style)}
};
</script></body></html>""",
)

BODY_GUI_HTML = BODY_GUI_HTML.replace(
    "</body></html>",
    """<script>
const renderMapWithAlignedHeading=renderMap;
renderMap=function(w){
  renderMapWithAlignedHeading(w);
  const sim=w?.sim||{},heading=w?.mode==='sim'?Number(sim.heading_deg||0)*Math.PI/180:Number(w?.perception?.body?.orientation||0),rotation=w?.mode==='sim'&&Number.isFinite(Number(sim.svg_heading_deg))?Number(sim.svg_heading_deg):90-heading*180/Math.PI;
  for(const svg of [document.getElementById('map'),document.getElementById('map-large')]){
    const pose=svg?.querySelector('.body-pose');if(!pose)continue;
    const nose=pose.querySelector('.heading');if(nose)nose.setAttribute('d','M0 -.72 L-.19 .19 L0 .08 L.19 .19 Z');
    pose.setAttribute('transform',`${pose.getAttribute('transform').split(' rotate(')[0]} rotate(${rotation})`);
  }
  if(w?.mode==='sim'){
    const step=document.getElementById('wm-steps');if(step)step.textContent=String(sim.steps??0);
    const label=step?.closest('.stat')?.querySelector('span');if(label)label.textContent='Simulation steps';
    const note=`Simulator ground truth · step ${sim.steps??0} · ${Object.keys(sim.objects||{}).length} objects · near misses ${sim.near_misses??0}`;
    for(const id of ['map-legend','map-legend-large']){const foot=document.getElementById(id)?.querySelector('.legend-foot');if(foot)foot.textContent=note}
  }
};

syncMapPose=function(pose){
  if(!pose?.body)return;
  const [x,y]=pose.body,heading=Number(pose.heading_deg||0),rotation=Number.isFinite(Number(pose.svg_heading_deg))?Number(pose.svg_heading_deg):90-heading;
  for(const svg of [document.getElementById('map'),document.getElementById('map-large')]){
    if(!svg)continue;
    const [, , width, height]=svg.getAttribute('viewBox').split(/\\s+/).map(Number),px=Math.max(.25,Math.min(width-.25,Number(x))),py=height-Math.max(.25,Math.min(height-.25,Number(y))),body=svg.querySelector('.body-pose'),reach=svg.querySelector('.reach');
    if(body){const nose=body.querySelector('.heading');if(nose)nose.setAttribute('d','M0 -.72 L-.19 .19 L0 .08 L.19 .19 Z');body.setAttribute('transform',`translate(${px} ${py}) rotate(${rotation})`)}
    if(reach){reach.setAttribute('cx',px);reach.setAttribute('cy',py)}
  }
};

const previousWorldRender=renderWorld;
renderWorld=function(w){previousWorldRender(w);if(w?.mode==='sim'&&w.sim)syncMapPose({body:w.sim.body,heading_deg:w.sim.heading_deg,svg_heading_deg:w.sim.svg_heading_deg})};

const previousFnkRender=drawFnkController;
drawFnkController=function(s){
  previousFnkRender(s);
  if(!s?.active)return;
  const svg=document.getElementById('fnk-preview'),legs=s.visual_legs||[];
  if(!svg||legs.length!==6)return;
  const heading=Number(s.body_heading_deg||0),rotation=Number.isFinite(Number(s.svg_heading_deg))?Number(s.svg_heading_deg):90-heading;
  const layout=[
    {name:'L1',x:158,y:88,side:-1},{name:'L2',x:153,y:118,side:-1},{name:'L3',x:158,y:148,side:-1},
    {name:'L4',x:202,y:88,side:1},{name:'L5',x:207,y:118,side:1},{name:'L6',x:202,y:148,side:1}
  ];
  const legSvg=layout.map((point,index)=>{
    const leg=legs[index],j=leg.joint_targets||[0,0,0],motion=leg.foot_motion||[0,0,0],lift=Number(leg.foot_lift||0),stance=Number(leg.contact)===1;
    const turning=String(s.gait||'').startsWith('turn_'),motionStride=Number(motion[0]||0),stride=motionStride||Number(leg.cpg||0),hipX=point.x,hipY=point.y;
    const coxaX=hipX+point.side*(12+Number(j[0]||0)*3),coxaY=hipY;
    const kneeX=coxaX+point.side*(19+Number(j[1]||0)*3),kneeY=coxaY+(turning?0:-stride*5);
    const footX=kneeX+point.side*(19+lift*5)+(turning?Number(motion[0]||0)*22:0);
    const footY=kneeY+(turning?Number(motion[1]||0)*22:-stride*8+Number(j[2]||0)*3)-lift*(turning?10:12);
    const color=stance?'#75dda4':'#f0bd70',dash=stance?'':'stroke-dasharray="4 3"';
    return `<g><line x1="${hipX}" y1="${hipY}" x2="${coxaX}" y2="${coxaY}" stroke="#83cbd1" stroke-width="6" stroke-linecap="round"/><line x1="${coxaX}" y1="${coxaY}" x2="${kneeX}" y2="${kneeY}" stroke="#9ce0b4" stroke-width="6" stroke-linecap="round"/><line x1="${kneeX}" y1="${kneeY}" x2="${footX}" y2="${footY}" stroke="${color}" stroke-width="5" stroke-linecap="round" ${dash}/><circle cx="${kneeX}" cy="${kneeY}" r="3.2" fill="#d9f4df"/><circle cx="${footX}" cy="${footY}" r="4.5" fill="${color}"/><text x="${footX+point.side*8}" y="${footY-5}" text-anchor="${point.side<0?'end':'start'}" fill="#dcebe2" font-size="9" font-family="monospace">${leg.name}</text></g>`;
  }).join('');
  const neuronSvg=legs.flatMap(leg=>leg.neuron_indices||[]).map((neuron,index)=>{
    const active=Boolean((s.spikes||[])[neuron]),legIndex=Math.floor(index/3),x=55+legIndex*50+(index%3)*7;
    return `<circle cx="${x}" cy="258" r="${active?4:2.5}" fill="${active?'#ffd27a':'#86cbd2'}" opacity="${active?1:.65}"/>`;
  }).join('');
  svg.setAttribute('viewBox','0 0 360 285');
  const actionLabel=s.world_action&&s.world_action!==s.gait?`${s.world_action} → ${s.gait}`:(s.gait||'idle');
  svg.innerHTML=`<text x="180" y="16" text-anchor="middle" fill="#dcebe2" font-size="9" font-family="monospace">N ↑</text><path d="M180 21l-5 9h10z" fill="#dcebe2"/><g transform="rotate(${rotation} 180 118)">${legSvg}<rect x="155" y="70" width="50" height="96" rx="10" fill="#152b22" stroke="#7ed9ab" stroke-width="2"/><text x="180" y="91" text-anchor="middle" fill="#8cae99" font-size="8" font-family="monospace">FRONT</text><text x="180" y="121" text-anchor="middle" fill="#c8f5d9" font-size="9" font-family="monospace">FNK0031</text></g><text x="180" y="198" text-anchor="middle" fill="#a7beaf" font-size="8" font-family="monospace">${actionLabel} · tripod A L1/L3/L5 · B L2/L4/L6</text><text x="180" y="238" text-anchor="middle" fill="#81998b" font-size="7" font-family="monospace">18 Izhikevich neurons · recent firing</text>${neuronSvg}`;
  document.getElementById('fnk-heading').textContent=`${Math.round(Number(s.compass_heading_deg??((90-heading+360)%360)))}°`;
  syncMapPose(s.worldmodel_pose);
};
</script></body></html>""",
)

BODY_GUI_HTML = BODY_GUI_HTML.replace(
    "</body></html>",
    """<script>
function renderPlanContract(payload){
  const panel=document.getElementById('plan-contract-panel');if(!panel)return;
  const plan=payload.active_plan;
  if(!plan){panel.innerHTML='<div class="panel-head"><h2>Body plan contract</h2><small>Phase 0</small></div><p class="muted">No active Brain plan. The Body is ready to validate a grounded plan against its latest scene.</p>';return}
  const step=(plan.steps||[])[plan.current_step_index||0];
  panel.innerHTML=`<div class="panel-head"><h2>Body plan contract</h2><small>${esc(plan.state)}</small></div><div class="meta"><span>Objective <b>${esc(plan.objective)}</b></span><span>Step <b>${Number(plan.current_step_index||0)+1}/${(plan.steps||[]).length}</b></span></div><p class="perception-text">${step?`${esc(step.verb)} ${esc(step.target||'')}`:'No pending step.'}</p><small class="muted">Validated against snapshot ${esc(payload.latest_snapshot?.snapshot_id||'—')}. Generic TaskGraph execution is next.</small>`;
}
async function refreshPlanContract(){try{renderPlanContract(await get('/worldmodel/plans'))}catch{}}
const planGrid=document.querySelector('#view-worldmodel .detail-grid');if(planGrid){const planPanel=document.createElement('section');planPanel.id='plan-contract-panel';planPanel.className='panel';planGrid.prepend(planPanel);refreshPlanContract();setInterval(refreshPlanContract,1500)}
</script></body></html>""",
)
